<%= form_with(model: subsector, local: true) do |form| %>
  <% if subsector.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(subsector.errors.count, "error") %> prohibited this subsector from being saved:</h2>

      <ul>
        <% subsector.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
